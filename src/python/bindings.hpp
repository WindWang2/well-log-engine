#pragma once

// Only headers that map to typesystem entries. Do not include TableModel until
// its generated wrapper is listed in WELLLOG_PYTHON_GENERATED_SOURCES.
#include <welllog/qtwidgets/well_log_view.hpp>
