---
status: accepted
---

# 测井曲线使用不可变且有所有权保证的共享缓冲区

WellLogDocument 的大数组以只读缓冲区视图提交，每个视图必须携带能覆盖内核读取周期的不透明生命周期令牌，禁止提交无所有权保证的裸指针。内核可零拷贝读取 NumPy、Arrow、mmap 或原生数组；已提交数组不得原地修改，曲线替换、分块追加和解释成果编辑通过新的 Document Revision 显式提交，从而保证异步抽稀与渲染期间的数据一致性。
