---
status: accepted
---

# 曲线采用分块层次包络 LOD

每条不可变曲线在后台建立分块 M4/Min-Max 摘要层次，摘要保留按源顺序排列的首点、极小点、极大点、末点及深度位置和缺测断点；渲染按局部 Display Depth 像素密度选择层级，细节足够时退回原始点。视口只上传选中的包络点，缓存以曲线版本和 Depth Transform 版本标识并可取消过期任务；逐视口全量扫描与 LTTB 不作为交互主路径，以保证一亿点负载下的帧预算和极值保真。
