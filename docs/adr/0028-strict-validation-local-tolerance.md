---
status: accepted
---

# 测井输入严格验证并局部容错

Sampling Axis 必须单调递增或递减，局部回跳由源适配器拆段而不静默排序，重复深度保留源顺序；Null Bitmap、NaN 和 Infinity 均不可绘制且必须断开曲线和填充，对数刻度的非正值跳过并计入诊断。区间要求 `top <= bottom`，零厚度使用 Marker 表达。无效单对象按 Entity ID、错误码和来源位置记录并跳过，只有结构性错误拒绝整个文档；渲染线程不因局部数据错误向宿主抛异常。
