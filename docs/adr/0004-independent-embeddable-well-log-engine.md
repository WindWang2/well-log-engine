---
status: accepted
---

# 独立且可嵌入的测井渲染内核

测井渲染能力建设为独立的 C++20 `WellLogEngine` SDK，而不是绑定到现有 Python、Qt 控件或某一种 GPU API。核心公共接口不暴露 Qt、Python 或图形后端对象；宿主通过适配层将同一内核用于独立桌面程序或嵌入 Python Qt 项目。这个边界以额外的适配层成本，换取无窗口测试、独立演进、跨宿主复用和渲染后端替换能力。
