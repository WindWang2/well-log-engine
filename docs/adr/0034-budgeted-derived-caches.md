---
status: accepted
---

# 派生 CPU/GPU 缓存显式预算化

原始只读缓冲区由数据所有者负责；CPU 派生缓存默认不超过原始缓冲区的 25%，GPU 缓存默认取可查询显存的 20% 与 512 MB 较小值、无法查询时为 256 MB。缓存覆盖可见区及上下约两个视口，以实体、文档、深度变换、样式和分辨率版本为键并按成本 LRU 淘汰；共享 Context Group 可共享字体与图纹 Atlas。内存压力先缩预取、再淘汰不可见几何和低价值层级，只能降低命中率，不能删除源数据或改变当前可见结果。
