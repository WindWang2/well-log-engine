---
status: accepted
---

# 首期扩展使用声明式 Custom Layer

首期公开扩展接口允许宿主通过受控的折线、三角形、区间、图像、文本、符号和裁切原语描述 Custom Layer，由内核统一执行批处理、拾取和矢量导出；扩展不得直接发出 OpenGL 调用或修改渲染状态。高性能专用 Layer Renderer 可随源码或同一工具链编译，但不承诺第三方 C++ 二进制 ABI；闭源二进制插件若成为真实需求，再以版本化 C ABI 单独设计。
